/**
 * 1 - Executar: shift + cmd + b
 * 2 - No terminal executar node + nome do arquivo sem a extensao
 */
export class OlaMundo {

    public digaOlaTodo(): void {
        console.log(`Fala coders`);
    }

}

let olaMundo = new OlaMundo();
olaMundo.digaOlaTodo();